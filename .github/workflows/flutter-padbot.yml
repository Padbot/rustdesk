name: Flutter PadBot Build

on:
  workflow_dispatch:

jobs:
  run-flutter-nightly-build:
    uses: ./.github/workflows/flutter-android.yml
    secrets: inherit
    with:
      upload-artifact: true
      upload-tag: "padbot"
